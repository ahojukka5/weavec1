(core-module
  (core-version 1)

  (decls

    (fn answer64
      (params)
      (returns i64)

      (body
        (return
          (const_i64 42))))

    (fn main
      (params)
      (returns i32)

      (body
        (return
          (cast_i64_to_i32
            (call_i64 answer64)))))))
