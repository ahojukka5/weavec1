# Build outputs (recreated by ./build.sh on every run).
# Includes build/vendor/weavec0 where the dependency is auto-fetched.
build/

# LLVM intermediates left around outside build/ for any reason.
*.bc
*.o

# Editor / OS noise.
.DS_Store
*.swp
*.swo
*~
